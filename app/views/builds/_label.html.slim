= link_to build, class: 'label-group', title: "#{Build.model_name.human} #{build.status.text}", data: {toggle: 'tooltip', placement: 'top'} do
  = tag_label icon_w(:gavel), :default
  = tag_label build.name, bs_context(build.status)
